import styles from './SlideItem.module.css';

const SlideItem = ({ item }) => {
    const { title, id, preview, backdrop_path } = item;

    return (
        <h1></h1>
    )
}

export default SlideItem
